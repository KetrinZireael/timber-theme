<section class="hero" style="background-image: url('{{ fields.background_image }}');">
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="hero__title">{{ fields.title }}</h1>
                <div class="mt-5">
                    <a href="{{ fields.cta_link }}" class="btn-custom">{{ fields.cta_title }}</a>
                </div>
            </div>
        </div>
    </div>
</section>